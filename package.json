{
  "name": "qiankun-demo",
  "version": "1.0.0",
  "description": "qiankun demo",
  "main": "index.js",
  "scripts": {
    "install": "npm-run-all --serial install:*",
    "start": "npm-run-all --parallel start:*",
    "start:main": "cd main && yarn start",
    "install:main": "cd main && yarn install",
    "start:react": "cd react-micro-app && yarn start",
    "install:react": "cd react-micro-app  && yarn install",
    "start:vue": "cd vuemicroapp && yarn start",
    "install:vue": "cd vuemicroapp && yarn install"
  },
  "keywords": [
    "qiankun"
  ],
  "author": "CP",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
